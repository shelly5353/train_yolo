# YOLO Shape Detection Dataset
# Classes: straight, L-shape, U-shape, complex

# Dataset paths - use absolute paths for Colab
path: /content/yolo_training_dataset  # dataset root dir
train: images/train  # train images (relative to 'path')
val: images/val  # val images (relative to 'path')

# Classes
nc: 4  # number of classes
names: ['straight', 'L-shape', 'U-shape', 'complex']  # class names
